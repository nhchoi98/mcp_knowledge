---
title: 운영체제(OS)
tags: []
created: ""
updated: ""
source: ""
status: draft
---
# 운영체제(OS)

# 운영체제란?

사용자에게 편리한 인터페이스 환경을 제공하고, 컴퓨터 시스템의 자원을 효율적으로 관리하는 소프트웨어.

정해진 방법으로만 컴퓨터 자원을 사용할 수 있도록 제한한다. 

## 인터페이스

컴퓨터를 사용할 수 있도록 해주고, 그 결과를 알려주는 것 

## 목적

자원 관리, 자원 보호, 하드웨어 인터페이스 제공, 사용자 인터페이스 제공 

# 커널

프로세스 관리, 메모리 관리, 저장장치 관리와 같은 운영체제의 핵심적인 기능을 모아놓은 것 

**운영체제의 두뇌**이자 **자원 관리자**로서, 하드웨어와 프로그램이 안정적이고 효율적으로 소통할 수 있도록 도와주는 핵심입니다.

1. **프로세스 관리**
    - 프로그램 실행(프로세스 생성/종료), 스케줄링(어떤 프로세스를 CPU에 할당할지 결정), 동기화와 프로세스간 통신 관리 등을 담당합니다.
2. **메모리 관리**
    - 각 프로그램이 사용할 수 있는 메모리 공간을 나누고, 필요할 때 할당·해제합니다.
    - 가상 메모리(virtual memory) 기법을 사용해 실제 메모리보다 큰 공간을 쓰는 것처럼 보이게 합니다.
3. **파일 시스템 관리**
    - 파일을 읽고 쓰고 삭제하는 등의 작업을 추상화하여 응용 프로그램이 디스크 구조를 몰라도 파일을 다룰 수 있게 합니다.
4. **디바이스 관리**
    - 키보드, 마우스, 프린터, 네트워크 카드 같은 하드웨어 장치 제어를 드라이버와 함께 관리합니다.
5. **보안과 보호**
    - 사용자 프로그램이 다른 프로그램이나 운영체제 영역을 함부로 침범하지 못하도록 메모리 보호, 접근 제어 등을 제공합니다.

---

### 커널의 종류

- **모놀리식 커널(Monolithic Kernel)**: 기능이 하나의 큰 커널 공간 안에서 동작 (ex: Linux, Unix)
- **마이크로커널(Microkernel)**: 최소한의 기능만 커널에 두고, 나머지는 사용자 공간에서 서비스 형태로 실행 (ex: Minix, 일부 현대 OS 실험적 구조)
- **하이브리드 커널(Hybrid Kernel)**: 위 두 방식을 절충 (ex: Windows NT, macOS XNU

## 시스템 호출 (System call)

커널이 자신을 보호하기 위해 만든 인터페이스. 응용프로그램이 하드웨어 자원에 접근하거나, 운영체제가 제공하는 서비스를 이용하려 할 때는 시스템 호출을 사용해야 한다. 쉽게 이야기하면 운영체제의 API를 시스템 호출이라고 정의할 수 있다. (커널과 응용프로그램의 인터페이스)

## 드라이버

커널과 하드웨어의 인터페이스는 드라이버가 담당한다. 

# 폰노이만 구조

오늘날의 컴퓨너틑 대부분 폰노이만 구조를 따른다.  CPU, 메모리, 입출력장치, 저장장치가 버스로 연결되어 있는 구조를 의미한다. 

![image.png](%EC%9A%B4%EC%98%81%EC%B2%B4%EC%A0%9C(OS)/image.png)

가장 중요한 특징은, 모든 프로그램은 메모리에 올라와야 실행할 수 있다는 것이다. 

[CPU와 메모리](%EC%9A%B4%EC%98%81%EC%B2%B4%EC%A0%9C(OS)/CPU%EC%99%80%20%EB%A9%94%EB%AA%A8%EB%A6%AC%20268cee38e89a806c95c9f4d9e14f76c8.md)

[프로세스와 쓰레드](%EC%9A%B4%EC%98%81%EC%B2%B4%EC%A0%9C(OS)/%ED%94%84%EB%A1%9C%EC%84%B8%EC%8A%A4%EC%99%80%20%EC%93%B0%EB%A0%88%EB%93%9C%20268cee38e89a8055b137ed18c9700de7.md)

# 버퍼

운영체제에서 버퍼는 데이터를 잠시 저장해두는 임시 저장 공간이다. 생산자와 소비자 사이에서 속도 차이를 완화하기 위해 사용되고, 데이터 전송 속도 불균형을 맞추기 위해 사용됨. 

👉 버퍼는 **흐름 제어용**, 캐시는 **속도 향상용**이라는 차이가 있습니다.

## 스풀 (SPOOL)

입출력 작업을 요청받은 순서대로 큐(queue)에 저장하고, 장치가 처리 가능할 때 순차적으로 넘겨주는 기법

- **프린터 스풀링**: 여러 사용자가 동시에 인쇄 요청을 보내도,
    
    → 각 요청이 디스크(스풀 파일)에 저장되고,
    
    → 프린터는 하나씩 순서대로 출력
    

| 구분 | 버퍼(Buffer) 🗂 | 스풀(Spool) 🖨 |
| --- | --- | --- |
| 정의 | 데이터 임시 저장 공간 | I/O 요청을 큐에 저장 후 순차 실행 |
| 목적 | 속도 차이 보완, 단위 맞춤 | 공유 장치의 순차적 사용, 다중 사용자 지원 |
| 위치 | 메모리(작은 크기) | 디스크(큰 크기) |
| 처리 대상 | 단일 I/O 작업 | 여러 작업을 순서대로 |
| 예시 | 출력 버퍼, 디스크 블록 버퍼 | 프린터 스풀링, 메일 스풀링 |

## 인터럽트

CPU가 현재 실행 중인 작업을 잠시 멈추고, 더 중요한 사건(Event)을 먼저 처리하도록 신호를 보내는 메커니즘

## 🔹 동작 과정 (간단 순서)

1. **이벤트 발생**
    
    (예: 키보드를 누름, 디스크 읽기 완료, 네트워크 패킷 도착)
    
2. **인터럽트 요청(IRQ)**
    
    → 장치가 CPU에 신호 보냄
    
3. **현재 작업 저장**
    
    → CPU는 실행 중인 명령의 상태(레지스터, PC 등)를 저장 (context 저장)
    
4. **인터럽트 서비스 루틴(ISR) 실행**
    
    → OS 안에 정의된 “이 인터럽트가 발생하면 어떻게 처리할지” 코드 실행
    
5. **원래 작업 복귀**
    
    → 인터럽트 처리가 끝나면 저장했던 상태를 복구하고, 중단했던 작업 이어서 실행
    

## 🔹 예시

- **키보드 입력**:
    
    사용자가 키보드를 치면 → 키보드 컨트롤러가 인터럽트 발생 → CPU가 ISR 실행 → 입력 버퍼에 키코드 저장.
    
- **프린터 완료 신호**:
    
    프린트 데이터 출력 끝 → 프린터가 인터럽트 발생 → CPU가 알려줌 → OS가 “출력 완료”로 사용자에게 반환.
    

## 🔹 인터럽트의 장점

- **효율성**: CPU가 쓸데없이 I/O 완료를 기다리지 않아도 됨.
- **동시성 지원**: 여러 장치와 CPU가 협력적으로 동작 가능.
- **즉시 대응**: 중요한 이벤트를 빠르게 처리 가능.

## 인터럽트와 폴링의 차이점

| 구분 | **폴링 (Polling)** | **인터럽트 (Interrupt)** |
| --- | --- | --- |
| **정의** | CPU가 주기적으로 장치의 상태를 직접 확인하는 방식 | 장치가 이벤트 발생 시 CPU에 직접 알려주는 방식 |
| **CPU 효율** | 비효율적 (계속 확인하느라 CPU 시간 낭비) | 효율적 (필요할 때만 CPU가 개입) |
| **응답 속도** | 느림 (CPU가 확인할 때까지 대기) | 빠름 (이벤트 즉시 처리) |
| **구현 난이도** | 단순 (루프만 돌면 됨) | 복잡 (인터럽트 벡터, ISR, 컨텍스트 스위칭 필요) |
| **사용 사례** | 작은 임베디드 시스템, 단순 장치 제어 | 현대 운영체제, 고성능 시스템, I/O 장치 처리 |
| **예시** | CPU가 계속 “키 눌렸나?” 확인 | 키보드가 눌리면 CPU에 바로 신호 보냄 |

## 🔹 비유로 이해하기

- **폴링**:
    
    선생님이 학생에게 “숙제 했니?” 하고 5초마다 계속 물어보는 것.
    
    (CPU가 장치 상태를 계속 확인)
    
- **인터럽트**:
    
    학생이 숙제 끝나면 “선생님, 다 했어요!” 하고 직접 알려주는 것.
    
    (장치가 CPU에 이벤트 알림)