---
title: React 렌더링 개요
tags: []
created: ""
updated: ""
source: ""
status: draft
---
title: React 렌더링 개요

# React 렌더링 개요

## 렌더링 파이프라인 (Trigger → Render → Commit → Paint)
- Trigger: state/props 변경, 부모 렌더 등
- Render: VDOM 생성 및 diff
- Commit: 실제 DOM 반영, useLayoutEffect
- Paint: 브라우저 페인트

## Reconciler 핵심
- 얕은 비교로 변경 탐지, 필요한 부분만 업데이트
- 이벤트 위임: 루트에서 핸들러 관리
- Fiber: Render phase 중단/재개 가능, Commit phase 일괄 처리

## 리렌더링 줄이기
- 참조 고정: useMemo/useCallback, stable deps
- 메모이제이션: React.memo, 선택적 memo
- 상태 배치/분리: context 남용 지양, 상태 colocation

## Suspense/Concurrent 포인트
- Suspense: 데이터/코드 로딩 경계, fallback 제공
- Transitions: 긴 작업을 낮은 우선순위로 내려 UI 응답성 유지

## 최적화 체크리스트
- [ ] 리렌더 원인 점검(state/props/parent)
- [ ] 메모이제이션/코드 분할 적용
- [ ] Suspense/transition 설계 여부
- [ ] Render-Commit 경계에서 부하 작업 제거

## 관련 문서
- [React 렌더링 파이프라인](react-rendering-pipeline.md)
- [i18n 렌더링 최적화](i18n-rendering-optimization.md)
- [Vercel optimizer/agent](vercel-optimizer-agent.md)

---

## 🎯 Suspense 핵심 개념

### Suspense란?

**Suspense**는 React에서 비동기 작업(데이터 페칭, 코드 로딩 등)이 완료될 때까지 **대체 UI(fallback)**를 표시하고, 준비되면 **실제 콘텐츠를 스트리밍**하는 메커니즘입니다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Suspense 동작 원리                               │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │    Suspense     │
                    │    Boundary     │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
      ┌───────────────┐           ┌───────────────┐
      │   Promise     │           │   Fallback    │
      │   Pending     │    →      │   표시됨       │
      └───────────────┘           └───────────────┘
              │
              ▼ (Promise 해결)
      ┌───────────────┐
      │   Children    │
      │   렌더링      │
      └───────────────┘

```

### Suspense가 해결하는 문제

| 기존 문제 | Suspense 해결책 |
|: ----------|:----------------|
| 로딩 상태 분산 관리 | 선언적 로딩 경계 |
| 워터폴 현상 | 병렬 스트리밍 |
| 전체 페이지 블로킹 | 점진적 렌더링 |
| 복잡한 조건부 렌더링 | 자동 fallback 처리 |

---

## 🌊 스트리밍(Streaming) 아키텍처

### HTML 스트리밍 동작 방식

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    스트리밍 vs 기존 방식 비교                             │
└─────────────────────────────────────────────────────────────────────────┘

기존 방식 (Blocking)
─────────────────────────────────────────────────────────────────────────→
│<──── 모든 데이터 페칭 ────>│<── 전체 HTML 전송 ──>│<─ 렌더링 ─>│
                                                              ↑
                                                         사용자 인식

스트리밍 방식 (Non-Blocking)
─────────────────────────────────────────────────────────────────────────→
│<─ 즉시 HTML 쉘 전송 ─>│
          ↑              │<── Chunk 1 ──>│<── Chunk 2 ──>│<── Chunk 3 ──>│
    사용자 인식

```

### 실제 스트리밍 예제

```tsx
// 페이지 구조
function Page() {
  return (
    <div>
      {/* 1️⃣ 즉시 렌더링 */}
      <Header />

      {/* 2️⃣ 데이터 로드 후 스트리밍 */}
      <Suspense fallback={<ProductSkeleton />}>
        <ProductList />
      </Suspense>

      {/* 3️⃣ 즉시 렌더링 */}
      <Suspense fallback={<ReviewSkeleton />}>
        <Reviews />
      </Suspense>

      {/* 4️⃣ 즉시 렌더링 */}
      <Footer />
    </div>
  )
}

```

**HTML 스트리밍 순서:**

```html
<!-- 1️⃣ 초기 HTML (즉시 전송) -->
<div>
  <header>...</header>
  <div id="product-fallback">Loading products...</div>
  <div id="review-fallback">Loading reviews...</div>
  <footer>...</footer>
</div>

<!-- 2️⃣ ProductList 준비 완료 (스트리밍) -->
<script>
  $RC("product-fallback", "<div class='products'>... </div>")
</script>

<!-- 3️⃣ Reviews 준비 완료 (스트리밍) -->
<script>
  $RC("review-fallback", "<div class='reviews'>...</div>")
</script>

```

---

## 📦 Suspense Boundary 전략

### 전략적 Suspense 배치

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Suspense Boundary 전략                               │
└─────────────────────────────────────────────────────────────────────────┘

세분화된 Boundary (권장)              단일 Boundary (비권장)
┌─────────────────────┐              ┌─────────────────────┐
│     Header (즉시)    │              │                     │
├─────────────────────┤              │                     │
│  ┌─────────────────┐ │              │   ┌───────────────┐ │
│  │ Suspense       │ │              │   │   전체 로딩    │ │
│  │  └─ Product    │ │              │   │   Spinner     │ │
│  └─────────────────┘ │              │   └───────────────┘ │
│  ┌─────────────────┐ │              │                     │
│  │ Suspense       │ │              │                     │
│  │  └─ Reviews    │ │              │                     │
│  └─────────────────┘ │              │                     │
├─────────────────────┤              │                     │
│     Footer (즉시)    │              └─────────────────────┘
└─────────────────────┘

✅ 각 섹션 독립적 로딩            ❌ 모든 데이터 대기 후 표시
✅ 빠른 First Paint             ❌ 느린 First Paint

```

### 🔴 잘못된 패턴 vs 🟢 올바른 패턴

### 패턴 1: 전체 페이지 블로킹

```tsx
// ❌ 잘못된 패턴:  전체 페이지가 데이터를 기다림
async function Page() {
  const data = await fetchData()  // 🔴 전체 페이지 블로킹

  return (
    <div>
      <Sidebar />      {/* 데이터 없이도 렌더 가능하지만 대기 */}
      <Header />       {/* 데이터 없이도 렌더 가능하지만 대기 */}
      <DataDisplay data={data} />
      <Footer />       {/* 데이터 없이도 렌더 가능하지만 대기 */}
    </div>
  )
}

```

```tsx
// ✅ 올바른 패턴:  데이터 필요한 부분만 대기
function Page() {
  return (
    <div>
      <Sidebar />      {/* 🟢 즉시 렌더링 */}
      <Header />       {/* 🟢 즉시 렌더링 */}
      <Suspense fallback={<Skeleton />}>
        <DataDisplay />  {/* 🟡 데이터 로드 후 스트리밍 */}
      </Suspense>
      <Footer />       {/* 🟢 즉시 렌더링 */}
    </div>
  )
}

async function DataDisplay() {
  const data = await fetchData()  // 이 컴포넌트만 블로킹
  return <div>{data. content}</div>
}

```

### 패턴 2: Promise 공유

```tsx
// ✅ 고급 패턴: Promise를 여러 컴포넌트에서 공유
function Page() {
  // 페칭 시작 (await 없이)
  const dataPromise = fetchData()

  return (
    <div>
      <Header />
      <Suspense fallback={<Skeleton />}>
        {/* 같은 Promise를 공유 → 한 번만 페칭 */}
        <DataDisplay dataPromise={dataPromise} />
        <DataSummary dataPromise={dataPromise} />
      </Suspense>
      <Footer />
    </div>
  )
}

function DataDisplay({ dataPromise }: { dataPromise: Promise<Data> }) {
  const data = use(dataPromise)  // React 19 use() 훅
  return <div>{data.content}</div>
}

function DataSummary({ dataPromise }: { dataPromise:  Promise<Data> }) {
  const data = use(dataPromise)  // 같은 Promise 재사용
  return <div>{data.summary}</div>
}

```

### `use()` 훅 상세 설명

```tsx
// use() 훅은 Promise를 컴포넌트 내에서 "unwrap" 합니다
function MyComponent({ dataPromise }) {
  // 1. Promise가 pending이면 → Suspense가 fallback 표시
  // 2. Promise가 resolved이면 → 값 반환
  // 3. Promise가 rejected이면 → Error Boundary로 전파
  const data = use(dataPromise)

  return <div>{data}</div>
}

```

---

## 🔄 Server Components와 Suspense

### RSC 렌더링 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│               React Server Components 렌더링 흐름                        │
└─────────────────────────────────────────────────────────────────────────┘

          Server Component                    Client Component
          (서버에서 실행)                      (브라우저에서 실행)
               │                                    │
               ▼                                    │
        ┌─────────────┐                            │
        │ async/await │                            │
        │ 데이터 페칭  │                            │
        └──────┬──────┘                            │
               │                                    │
               ▼                                    │
        ┌─────────────┐                            │
        │   RSC       │     직렬화된 Props         │
        │  Payload    │ ────────────────────────→  │
        │ (직렬화)    │                            │
        └─────────────┘                            ▼
                                            ┌─────────────┐
                                            │   Client    │
                                            │  Hydration  │
                                            └─────────────┘

```

### RSC에서 병렬 데이터 페칭

```tsx
// ❌ 잘못된 패턴: 순차 실행 (워터폴)
export default async function Page() {
  const header = await fetchHeader()   // 1초
  const sidebar = await fetchSidebar() // 1초
  // 총 2초 대기

  return (
    <div>
      <div>{header}</div>
      <Sidebar data={sidebar} />
    </div>
  )
}

```

```tsx
// ✅ 올바른 패턴: 컴포넌트 분리로 병렬 실행
async function Header() {
  const data = await fetchHeader()  // 1초
  return <div>{data}</div>
}

async function Sidebar() {
  const items = await fetchSidebar()  // 1초 (Header와 병렬)
  return <nav>{items. map(renderItem)}</nav>
}

// 부모는 동기 컴포넌트
export default function Page() {
  return (
    <div>
      <Header />   {/* 비동기, 병렬 시작 */}
      <Sidebar />  {/* 비동기, 병렬 시작 */}
    </div>
  )
}
// 총 1초 대기 (병렬 실행)

```

### RSC 경계에서 직렬화 최적화

```tsx
// ❌ 불필요한 데이터 직렬화
async function Page() {
  const user = await fetchUser()  // 50개 필드
  return <Profile user={user} />  // 전체 객체 직렬화
}

'use client'
function Profile({ user }: { user: User }) {
  return <div>{user.name}</div>  // 1개 필드만 사용
}

```

```tsx
// ✅ 필요한 데이터만 직렬화
async function Page() {
  const user = await fetchUser()
  // 필요한 것만 전달
  return <Profile name={user.name} avatar={user.avatar} />
}

'use client'
function Profile({ name, avatar }: { name:  string; avatar: string }) {
  return (
    <div>
      <img src={avatar} alt={name} />
      <span>{name}</span>
    </div>
  )
}

```

---

## ⚡ Hydration 최적화

### Hydration 프로세스

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Hydration 프로세스                                  │
└─────────────────────────────────────────────────────────────────────────┘

시간 →
────────────────────────────────────────────────────────────────────────────

1️⃣ HTML 수신        2️⃣ JS 로드         3️⃣ Hydration       4️⃣ 인터랙티브
┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│              │   │              │   │              │   │              │
│   정적 HTML  │ → │  React 번들  │ → │  이벤트 연결  │ → │   완전한     │
│   (보기만)   │   │    로딩      │   │  상태 복원   │   │   React App  │
│              │   │              │   │              │   │              │
└──────────────┘   └──────────────┘   └──────────────┘   └──────────────┘
       ↑                                                        ↑
   First Paint                                              TTI (Time to Interactive)

```

### Hydration Mismatch 방지

```tsx
// ❌ 잘못된 패턴: SSR에서 localStorage 접근 → 에러
function ThemeWrapper({ children }) {
  // 서버에서는 localStorage가 없음!
  const theme = localStorage.getItem('theme') || 'light'
  return <div className={theme}>{children}</div>
}

```

```tsx
// ❌ 잘못된 패턴:  useEffect로 처리 → 깜빡임
function ThemeWrapper({ children }) {
  const [theme, setTheme] = useState('light')

  useEffect(() => {
    const stored = localStorage.getItem('theme')
    if (stored) setTheme(stored)  // 🔴 화면 깜빡임!
  }, [])

  return <div className={theme}>{children}</div>
}

```

```tsx
// ✅ 올바른 패턴:  인라인 스크립트로 깜빡임 없이 처리
function ThemeWrapper({ children }) {
  return (
    <>
      <div id="theme-wrapper">{children}</div>
      <script
        dangerouslySetInnerHTML={{
          __html: `
            (function() {
              try {
                var theme = localStorage.getItem('theme') || 'light';
                var el = document.getElementById('theme-wrapper');
                if (el) el.className = theme;
              } catch (e) {}
            })();
          `,
        }}
      />
    </>
  )
}

```

**동작 원리:**

1. 서버: 기본 HTML 전송 (클래스 없음)
2. 브라우저: HTML 파싱 중 인라인 스크립트 **동기** 실행
3. DOM에 클래스 적용 **후** 화면에 표시
4. → 깜빡임 없음!

---

## 🔀 Concurrent Features

### Transitions

**Transitions**는 긴급하지 않은 상태 업데이트를 표시하여 UI 반응성을 유지합니다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     Transition 동작 원리                                │
└─────────────────────────────────────────────────────────────────────────┘

사용자 입력 (긴급)                    결과 렌더링 (비긴급)
      │                                     │
      ▼                                     ▼
┌─────────────┐                     ┌─────────────┐
│ 입력 필드   │   startTransition   │ 필터링된    │
│ 즉시 반영   │ ─────────────────→  │ 목록 표시   │
│             │    (중단 가능)       │             │
└─────────────┘                     └─────────────┘

```

```tsx
import { useState, useTransition } from 'react'

function SearchPage() {
  const [inputValue, setInputValue] = useState('')
  const [searchResults, setSearchResults] = useState([])
  const [isPending, startTransition] = useTransition()

  const handleSearch = (query:  string) => {
    // 1️⃣ 긴급: 입력 필드 즉시 업데이트
    setInputValue(query)

    // 2️⃣ 비긴급: 검색 결과는 Transition으로
    startTransition(() => {
      const filtered = filterItems(query)  // 무거운 연산
      setSearchResults(filtered)
    })
  }

  return (
    <div>
      <input
        value={inputValue}
        onChange={(e) => handleSearch(e.target.value)}
      />
      {isPending && <Spinner />}
      <ResultList results={searchResults} />
    </div>
  )
}

```

### Suspense + Transitions 조합

```tsx
function App() {
  const [tab, setTab] = useState('home')
  const [isPending, startTransition] = useTransition()

  const handleTabChange = (newTab: string) => {
    startTransition(() => {
      setTab(newTab)
    })
  }

  return (
    <div>
      <TabButtons
        activeTab={tab}
        onTabChange={handleTabChange}
        isPending={isPending}
      />
      <Suspense fallback={<TabSkeleton />}>
        {tab === 'home' && <HomePage />}
        {tab === 'about' && <AboutPage />}
        {tab === 'contact' && <ContactPage />}
      </Suspense>
    </div>
  )
}

```

---

## 🎭 Activity Component (React 19+)

### 상태 보존 렌더링

```tsx
import { Activity } from 'react'

function TabPanel({ tabs, activeTab }) {
  return (
    <div>
      {tabs.map((tab) => (
        <Activity
          key={tab.id}
          mode={tab.id === activeTab ? 'visible' : 'hidden'}
        >
          <ExpensiveTabContent tab={tab} />
        </Activity>
      ))}
    </div>
  )
}

```

**Activity vs 조건부 렌더링:**

| 특성 | Activity (hidden) | 조건부 렌더링 |
|: -----|:------------------|:--------------|
| DOM 유지 | ✅ 유지 | ❌ 제거 후 재생성 |
| 상태 유지 | ✅ 유지 | ❌ 초기화 |
| 메모리 사용 | 더 많음 | 적음 |
| 전환 속도 | 즉시 | 리렌더링 필요 |

---

## 📊 Suspense 사용 시점 결정 가이드

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   Suspense 사용 결정 플로우차트                          │
└─────────────────────────────────────────────────────────────────────────┘

                    데이터 페칭이 필요한가?
                           │
              ┌────────────┴────────────┐
              │                         │
             Yes                        No
              │                         │
              ▼                         ▼
    레이아웃에 영향을 주는가?      Suspense 불필요
              │
    ┌─────────┴─────────┐
    │                   │
   Yes                  No
    │                   │
    ▼                   ▼
SEO가 중요한 콘텐츠인가?    ✅ Suspense 사용 권장
    │                        (점진적 로딩)
    │
  ┌─┴─┐
  │   │
 Yes  No
  │   │
  ▼   ▼
await 사용   ✅ Suspense 사용 권장
(Blocking)

```

### Suspense 사용 권장 상황

| 상황 | 권장 | 이유 |
|: -----|:-----|:-----|
| 상품 목록 | ✅ Suspense | 헤더/푸터 먼저 표시 |
| 댓글 섹션 | ✅ Suspense | 본문 먼저 표시 |
| 대시보드 위젯 | ✅ Suspense | 각 위젯 독립 로딩 |
| SEO 메타데이터 | ❌ await | 크롤러가 읽어야 함 |
| 레이아웃 결정 데이터 | ❌ await | 레이아웃 시프트 방지 |
| 빠른 쿼리 (<50ms) | ❌ await | Suspense 오버헤드 > 이득 |

---

## 🛠️ 실전 패턴 모음

### 1. 중첩 Suspense (Nested Suspense)

```tsx
function Dashboard() {
  return (
    <div className="dashboard">
      {/* 1단계: 전체 대시보드 쉘 */}
      <Suspense fallback={<DashboardSkeleton />}>
        <DashboardShell>
          {/* 2단계: 개별 위젯 */}
          <Suspense fallback={<ChartSkeleton />}>
            <RevenueChart />
          </Suspense>

          <Suspense fallback={<TableSkeleton />}>
            <RecentOrders />
          </Suspense>

          <Suspense fallback={<ListSkeleton />}>
            <TopProducts />
          </Suspense>
        </DashboardShell>
      </Suspense>
    </div>
  )
}

```

### 2. Error Boundary + Suspense 조합

```tsx
import { ErrorBoundary } from 'react-error-boundary'

function ProductPage({ productId }) {
  return (
    <ErrorBoundary
      fallback={<ErrorMessage message="상품을 불러올 수 없습니다" />}
      onError={(error) => logError(error)}
    >
      <Suspense fallback={<ProductSkeleton />}>
        <ProductDetails id={productId} />
      </Suspense>
    </ErrorBoundary>
  )
}

```

### 3. Preloading + Suspense

```tsx
// 미리 데이터 페칭 시작
const productCache = new Map()

function preloadProduct(id:  string) {
  if (! productCache.has(id)) {
    productCache.set(id, fetchProduct(id))
  }
}

// 버튼에서 프리로드
function ProductCard({ product }) {
  return (
    <Link
      href={`/product/${product.id}`}
      onMouseEnter={() => preloadProduct(product.id)}
      onFocus={() => preloadProduct(product.id)}
    >
      {product.name}
    </Link>
  )
}

// 상세 페이지에서 캐시된 Promise 사용
function ProductDetails({ id }) {
  const productPromise = productCache.get(id) || fetchProduct(id)
  const product = use(productPromise)

  return <div>{product.name}</div>
}

```

---

## 📈 성능 측정 지표

### Core Web Vitals에 미치는 영향

| 지표 | Suspense 적용 전 | Suspense 적용 후 | 개선 |
| --- | --- | --- | --- |
| **LCP** (Largest Contentful Paint) | 3.2s | 1.8s | 44% ⬇️ |
| **FID** (First Input Delay) | 180ms | 45ms | 75% ⬇️ |
| **CLS** (Cumulative Layout Shift) | 0.25 | 0.08* | 68% ⬇️ |
- Skeleton UI를 적절히 디자인한 경우

---

## 📚 핵심 요약

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        핵심 체크리스트                                   │
└─────────────────────────────────────────────────────────────────────────┘

✅ Suspense Boundary를 세분화하여 독립적 로딩 구현
✅ RSC에서 async 컴포넌트 분리로 병렬 데이터 페칭
✅ use() 훅으로 Promise 공유하여 중복 페칭 방지
✅ RSC→Client 경계에서 최소한의 데이터만 직렬화
✅ Hydration mismatch는 인라인 스크립트로 해결
✅ useTransition으로 비긴급 업데이트 분리
✅ Activity 컴포넌트로 상태 보존 show/hide 구현
✅ Error Boundary와 Suspense 조합으로 에러 처리

```

---

*Based on [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices)*

# CSR vs SSR 심층 비교 및 Nginx 서빙 아키텍처

---

## 1️⃣ CSR vs SSR 핵심 차이점

### 렌더링 위치와 타이밍

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR (Client-Side Rendering)                          │
└─────────────────────────────────────────────────────────────────────────┘

서버                                    브라우저
┌──────────┐                           ┌──────────────────────────────────┐
│          │   빈 HTML + JS 번들       │  1. HTML 파싱 (빈 껍데기)        │
│  Nginx   │ ────────────────────────→ │  2. JS 다운로드                  │
│          │                           │  3. JS 실행 & React 초기화       │
│          │                           │  4. API 호출 (데이터 페칭)       │
│          │                           │  5. DOM 생성 & 렌더링            │
└──────────┘                           └──────────────────────────────────┘
                                              ↑
                                         모든 작업이 여기서 발생

┌─────────────────────────────────────────────────────────────────────────┐
│                    SSR (Server-Side Rendering)                          │
└─────────────────────────────────────────────────────────────────────────┘

서버                                    브라우저
┌──────────────────────────┐           ┌──────────────────────────────────┐
│  1. 요청 수신             │           │                                  │
│  2. 데이터 페칭           │  완성된   │  1. HTML 파싱 (콘텐츠 있음)      │
│  3. React 렌더링         │  HTML     │  2. 화면 표시 (즉시)              │
│  4. HTML 문자열 생성     │ ────────→ │  3. JS 다운로드                  │
│                          │           │  4. Hydration (이벤트 연결)      │
└──────────────────────────┘           └──────────────────────────────────┘
        ↑
   렌더링이 여기서 발생

```

### 시간순 비교

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        타임라인 비교                                     │
└─────────────────────────────────────────────────────────────────────────┘

시간 →  0ms      200ms     500ms     800ms    1200ms    1500ms    2000ms
        │         │         │         │         │         │         │

CSR:     │─ HTML ─│── JS 다운로드 ──│─ JS 실행 ─│─ API ─│─ 렌더링 ─│
        │        │                 │           │       │          │
        │        │                 │           │       │          ↑
        │        │                 │           │       │     First Paint
        │        │                 │           │       │     (콘텐츠 표시)
        │        │                 │           │       │
        ↑        │                 │           │       │
   빈 화면 표시   │                 │           │       │

SSR:    │─ 서버 렌더링 ─│─ HTML ─│── 화면 표시 ──│── JS ──│─ Hydration ─│
        │              │        │               │        │             │
        │              │        ↑               │        │             ↑
        │              │   First Paint          │        │         Interactive
        │              │   (콘텐츠 표시)         │        │

```

### 핵심 차이 요약

| 구분 | CSR | SSR |
|: -----|:----|:----|
| **렌더링 위치** | 브라우저 | 서버 |
| **초기 HTML** | 빈 `<div id="root">` | 완성된 콘텐츠 |
| **First Paint** | JS 실행 후 (느림) | HTML 수신 즉시 (빠름) |
| **TTI (Time to Interactive)** | First Paint와 동시 | Hydration 완료 후 |
| **SEO** | ❌ 크롤러가 콘텐츠 못 봄 | ✅ 완전한 HTML |
| **서버 부하** | 낮음 (정적 파일만) | 높음 (매 요청마다 렌더링) |
| **TTFB** | 매우 빠름 | 렌더링 시간만큼 지연 |
| **캐싱** | CDN 캐싱 용이 | 동적 콘텐츠 캐싱 어려움 |

---

## 2️⃣ CSR이 적합한 경우

### ✅ CSR을 선택해야 하는 상황

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     CSR 적합 사례                                        │
└─────────────────────────────────────────────────────────────────────────┘

1. 로그인 후 대시보드 / 관리자 페이지
   ├─ SEO 불필요 (검색엔진 노출 X)
   ├─ 인증된 사용자만 접근
   └─ 빈번한 인터랙션

2. SaaS 애플리케이션
   ├─ Notion, Figma, Slack 같은 앱
   ├─ 복잡한 상태 관리
   └─ 실시간 협업 기능

3. 내부 도구 / 백오피스
   ├─ 직원만 사용
   ├─ SEO 완전 불필요
   └─ 빠른 개발 속도 우선

4. 인터랙티브 위젯 / 임베드
   ├─ 다른 사이트에 삽입되는 컴포넌트
   ├─ 독립적으로 동작
   └─ 예: 채팅 위젯, 분석 대시보드

5. 오프라인 지원 PWA
   ├─ Service Worker와 결합
   ├─ 로컬 캐싱 전략
   └─ 네트워크 독립적 동작

```

### ❌ CSR을 피해야 하는 상황

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     CSR 부적합 사례                                      │
└─────────────────────────────────────────────────────────────────────────┘

1. 콘텐츠 중심 웹사이트
   ├─ 블로그, 뉴스, 문서 사이트
   ├─ SEO가 트래픽의 핵심
   └─ → SSG/SSR 권장

2. 이커머스 상품 페이지
   ├─ 검색 엔진 노출 필수
   ├─ 소셜 미디어 공유 메타태그
   └─ → SSR/ISR 권장

3. 마케팅 랜딩 페이지
   ├─ 첫 인상이 중요 (빠른 로딩)
   ├─ 검색 광고 품질 점수
   └─ → SSG 권장

4. 저사양 기기 대상 서비스
   ├─ JS 실행이 느림
   ├─ 서버에서 처리하는 게 유리
   └─ → SSR 권장

```

### 선택 가이드 플로우차트

```
                        SEO가 중요한가?
                              │
                ┌─────────────┴─────────────┐
                │                           │
               Yes                          No
                │                           │
                ▼                           ▼
        콘텐츠가 자주 변하나?          로그인 필요한가?
                │                           │
        ┌───────┴───────┐           ┌───────┴───────┐
        │               │           │               │
       Yes             No          Yes             No
        │               │           │               │
        ▼               ▼           ▼               ▼
      SSR            SSG/ISR      CSR ✅        복잡한 인터랙션?
                                                    │
                                            ┌───────┴───────┐
                                            │               │
                                           Yes             No
                                            │               │
                                            ▼               ▼
                                          CSR ✅          SSG

```

---

## 3️⃣ Nginx + CSR 전체 아키텍처

### 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR + Nginx 전체 아키텍처                             │
└─────────────────────────────────────────────────────────────────────────┘

사용자                           Nginx                         Origin
┌──────────┐                ┌─────────────┐               ┌─────────────┐
│          │                │             │               │             │
│ 브라우저  │ ←───────────→  │   Nginx     │ ←──(필요시)──→ │  API 서버   │
│          │                │  (정적서빙)  │               │  (Node.js)  │
│          │                │             │               │             │
└──────────┘                └─────────────┘               └─────────────┘
                                   │
                                   ▼
                            ┌─────────────┐
                            │  정적 파일   │
                            │  저장소      │
                            ├─────────────┤
                            │ index.html  │
                            │ main.js     │
                            │ main.css    │
                            │ assets/     │
                            └─────────────┘

```

### 요청-응답 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    상세 요청-응답 흐름                                    │
└──────────────────────────────────────────────────────────��──────────────┘

[1단계:  초기 페이지 요청]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사용자                    Nginx                      파일시스템
   │                        │                            │
   │  GET /dashboard        │                            │
   │ ─────────────────────→ │                            │
   │                        │  index.html 읽기            │
   │                        │ ──────────────────────────→ │
   │                        │                            │
   │                        │ ←────────────────────────── │
   │  200 OK                │                            │
   │  (index.html)          │                            │
   │ ←───────────────────── │                            │
   │                        │                            │

[2단계: 정적 자원 요청]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

브라우저                   Nginx                      파일시스템
   │                        │                            │
   │  GET /static/main.js   │                            │
   │ ─────────────────────→ │                            │
   │                        │ ──────────────────────────→ │
   │ ←───────────────────── │ ←────────────────────────── │
   │  200 OK (gzip)         │                            │
   │                        │                            │
   │  GET /static/main.css  │                            │
   │ ─────────────────────→ │                            │
   │ ←───────────────────── │ (동시에 여러 요청 처리)      │
   │                        │                            │

[3단계: API 요청 (프록시)]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

React App                  Nginx                      API 서버
   │                        │                            │
   │  GET /api/users        │                            │
   │ ─────────────────────→ │  프록시 패스               │
   │                        │ ──────────────────────────→ │
   │                        │                            │
   │                        │ ←────────────────────────── │
   │  200 OK                │                            │
   │  (JSON data)           │                            │
   │ ←───────────────────── │                            │

```

---

## 4️⃣ 브라우저 내부 동작 상세

### HTML 파싱부터 렌더링까지

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    브라우저 렌더링 파이프라인 (CSR)                       │
└─────────────────────────────────────────────────────────────────────────┘

1. HTML 수신
   │
   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  <! DOCTYPE html>                                                        │
│  <html>                                                                 │
│    <head>                                                               │
│      <link rel="stylesheet" href="/static/main.css">                   │
│      <script defer src="/static/main.js"></script>                     │
│    </head>                                                              │
│    <body>                                                               │
│      <div id="root"></div>  ← 빈 컨테이너                               │
│    </body>                                                              │
│  </html>                                                                │
└─────────────────────────────────────────────────────────────────────────┘
   │
   ▼
2. DOM 트리 구성
   │
   ▼
┌───────────────────────┐
│       Document        │
│          │            │
│        html           │
│       /    \\          │
│    head    body       │
│     │        │        │
│   link    div#root    │  ← 여기에 React가 마운트됨
│   script    (빈)      │
└───────────────────────┘
   │
   ▼
3. CSS 다운로드 & CSSOM 구성
   │
   ▼
4. JavaScript 다운로드 (defer)
   │
   ▼
5. DOMContentLoaded 이벤트
   │
   ▼
6. JavaScript 실행 시작

```

### React 초기화 과정

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    React 초기화 (CSR)                                    │
└─────────────────────────────────────────────────────────────────────────┘

// main.js 실행

1. React 런타임 로드
   │
   ▼
┌─────────────────────────────────────────────┐
│  import React from 'react'                  │
│  import { createRoot } from 'react-dom/client'│
│  import App from './App'                    │
└─────────────────────────────────────────────┘
   │
   ▼
2. Root 컨테이너 선택
   │
   ▼
┌─────────────────────────────────────────────┐
│  const container = document.getElementById('root')│
│  const root = createRoot(container)         │
└─────────────────────────────────────────────┘
   │
   ▼
3. 컴포넌트 트리 렌더링
   │
   ▼
┌─────────────────────────────────────────────┐
│  root.render(<App />)                       │
│                                             │
│  // React가 내부적으로:                       │
│  // 1. 컴포넌트 함수 호출                    │
│  // 2. Virtual DOM 생성                     │
│  // 3. 실제 DOM 생성 & 삽입                  │
└─────────────────────────────────────────────┘
   │
   ▼
4. 데이터 페칭 (useEffect)
   │
   ▼
┌─────────────────────────────────────────────┐
│  useEffect(() => {                          │
│    fetch('/api/data')                       │
│      .then(res => res.json())               │
│      .then(setData)                         │
│  }, [])                                     │
└─────────────────────────────────────────────┘
   │
   ▼
5. 상태 업데이트 → 리렌더링
   │
   ▼
┌─────────────────────────────────────────────┐
│  // setData(newData) 호출 시:                │
│  // 1. 새 Virtual DOM 생성                  │
│  // 2. Diffing (변경점 계산)                │
│  // 3. 변경된 부분만 실제 DOM 업데이트       │
└─────────────────────────────────────────────┘

```

### 네트워크 워터폴 시각화

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR 네트워크 워터폴                                   │
└─────────────────────────────────────────────────────────────────────────┘

시간 →   0      100     200     300     400     500     600     700ms
         │       │       │       │       │       │       │       │

index.html ████│
                │
main.css       │████████│
                │       │
main.js        │████████████████████│
                │                   │
                │                   │ (JS 실행)
                │                   │████│
                │                        │
/api/user      │                        │████████████│
                │                                    │
/api/posts     │                        │████████████████│
                │                                        │
                │                                        │
                │                                        ↓
                │                                   First Paint
                │                                   (콘텐츠 표시)
                │
                ↓
           First Paint
           (빈 화면/로딩)

────────────────────────────────────────────────────────────────────────────
                    Critical Rendering Path

```

---

## 5️⃣ Nginx 설정 상세

### 기본 CSR 서빙 설정

```
# /etc/nginx/sites-available/react-app

server {
    listen 80;
    server_name example.com;

    # React 빌드 결과물 위치
    root /var/www/react-app/build;
    index index.html;

    # Gzip 압축 활성화
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;

    # 정적 자원 캐싱 (해시��� 포함된 파일명)
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 프록시
    location /api/ {
        proxy_pass <http://localhost:3001>;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    # SPA 라우팅:  모든 경로를 index.html로
    location / {
        try_files $uri $uri/ /index.html;
    }
}

```

### try_files 동작 원리

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    try_files 동작 흐름                                   │
└─────────────────────────────────────────────────────────────────────────┘

요청:  GET /dashboard/settings
              │
              ▼
┌─────────────────────────────────────────┐
│  try_files $uri $uri/ /index.html       │
└─────────────────────────────────────────┘
              │
              ▼
1️⃣ $uri 확인:  /var/www/build/dashboard/settings 파일 존재?
              │
              ├─ Yes → 해당 파일 반환
              │
              └─ No ↓

2️⃣ $uri/ 확인: /var/www/build/dashboard/settings/ 디렉토리 존재?
              │
              ├─ Yes → 디렉토리의 index.html 반환
              │
              └─ No ↓

3️⃣ /index.html 반환 (Fallback)
              │
              ▼
┌─────────────────────────────────────────┐
│  /var/www/build/index.html 반환         │
│                                         │
│  → React Router가 /dashboard/settings   │
│    경로를 처리                           │
└─────────────────────────────────────────┘

왜 필요한가?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SPA에서 /dashboard/settings 같은 경로는:
- 실제 파일이 아님 (서버에 없음)
- React Router가 클라이언트에서 처리하는 가상 경로
- 따라서 항상 index.html을 반환해야 React가 라우팅 처리 가능

try_files가 없으면?
→ 404 Not Found 에러 발생

```

### 캐싱 전략

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR 캐싱 전략                                         │
└─────────────────────────────────────────────────────────────────────────┘

파일 유형별 캐싱 정책:

┌──────────────────┬─────────────────┬────────────────────────────────────┐
│      파일        │    캐시 정책     │              이유                  │
├──────────────────┼─────────────────┼────────────────────────────────────┤
│ index.html       │ no-cache        │ 항상 최신 버전 확인 필요           │
│                  │ (매 요청 검증)   │ 새 배포 시 즉시 반영               │
├──────────────────┼─────────────────┼────────────────────────────────────┤
│ main.[hash].js   │ 1년 (immutable) │ 해시가 변경되면 새 파일            │
│ main.[hash].css  │                 │ 콘텐츠 변경 = 파일명 변경          │
├──────────────────┼─────────────────┼────────────────────────────────────┤
│ 이미지/폰트      │ 1년             │ 정적 자원, 변경 드묾               │
└──────────────────┴─────────────────┴────────────────────────────────────┘

왜 index.html은 캐싱하면 안 되나?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

배포 전:
index.html → <script src="/static/main.abc123.js">

배포 후:
index. html → <script src="/static/main.def456.js">  (새 해시)

만약 index.html이 캐싱되어 있다면:
- 사용자는 여전히 main.abc123.js를 요청
- 하지만 서버에는 main.def456.js만 존재
- → 404 에러 또는 구버전 앱 실행

```

```
# 캐싱 전략 상세 설정

# index.html - 캐시하지 않음
location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    add_header Pragma "no-cache";
    add_header Expires "0";
}

# 해시가 포함된 정적 자원 - 영구 캐시
location /static/ {
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
}

# 이미지, 폰트 등
location ~* \\.(jpg|jpeg|png|gif|ico|svg|woff|woff2)$ {
    expires 1y;
    add_header Cache-Control "public, max-age=31536000";
}

```

---

## 6️⃣ 전체 시퀀스 다이어그램

```
┌────────────────���────────────────────────────────────────────────────────┐
│                    CSR 전체 시퀀스 (상세)                                │
└─────────────────────────────────────────────────────────────────────────┘

  사용자        브라우저           Nginx           파일시스템      API서버
    │             │                 │                 │              │
    │ URL 입력    │                 │                 │              │
    │───────────→│                 │                 │              │
    │             │                 │                 │              │
    │             │ GET /dashboard  │                 │              │
    │             │────────────────→│                 │              │
    │             │                 │                 │              │
    │             │                 │ try_files       │              │
    │             │                 │ (파일 없음)      │              │
    │             │                 │────────────────→│              │
    │             │                 │                 │              │
    │             │                 │ index.html      │              │
    │             │                 │←────────────────│              │
    │             │                 │                 │              │
    │             │ 200 OK          │                 │              │
    │             │ (HTML)          │                 │              │
    │             │←────────────────│                 │              │
    │             │                 │                 │              │
    │             │ HTML 파싱 시작   │                 │              │
    │             │ ───────────     │                 │              │
    │             │                 │                 │              │
    │ 빈 화면     │                 │                 │              │
    │←───────────│                 │                 │              │
    │ (로딩 표시) │                 │                 │              │
    │             │                 │                 │              │
    │             │ GET /static/    │                 │              │
    │             │ main.abc123.js  │                 │              │
    │             │────────────────→│────────────────→│              │
    │             │                 │                 │              │
    │             │ 200 OK          │                 │              │
    │             │ (JS, gzip)      │                 │              │
    │             │←────────────────│←────────────────│              │
    │             │                 │                 │              │
    │             │ GET /static/    │                 │              │
    │             │ main. xyz789.css │                 │              │
    │             │────────────────→│────────────────→│              │
    │             │←────────────────│←────────────────│              │
    │             │                 │                 │              │
    │             │ JS 실행         │                 │              │
    │             │ React 초기화     │                 │              │
    │             │ ─────────────── │                 │              │
    │             │                 │                 │              │
    │             │ GET /api/user   │                 │              │
    │             │────────────────→│ proxy_pass      │              │
    │             │                 │─────────────────────────────→│
    │             │                 │                 │              │
    │             │                 │←─────────────────────────────│
    │             │ 200 OK (JSON)   │                 │              │
    │             │←────────────────│                 │              │
    │             │                 │                 │              │
    │             │ 상태 업데이트    │                 │              │
    │             │ DOM 렌더링      │                 │              │
    │             │ ─────────────── │                 │              │
    │             │                 │                 │              │
    │ 콘텐츠 표시 │                 │                 │              │
    │←───────────│                 │                 │              │
    │ (완료)      │                 │                 │              │

```

---

## 7️⃣ 성능 최적화 체크리스트

### CSR 최적화 포인트

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR 성능 최적화 체크리스트                             │
└─────────────────────────────────────────────────────────────────────────┘

[번들 최적화]
□ Code Splitting (React. lazy + Suspense)
□ Tree Shaking 활성화
□ 번들 분석 (webpack-bundle-analyzer)
□ 사용하지 않는 코드 제거

[네트워크 최적화]
□ Gzip/Brotli 압축 (Nginx)
□ HTTP/2 활성화
□ 정적 자원 CDN 배포
□ preload/prefetch 힌트

[캐싱 전략]
□ 해시 기반 파일명 (cache busting)
□ Service Worker (오프라인 지원)
□ API 응답 캐싱 (SWR/React Query)

[로딩 최적화]
□ 크리티컬 CSS 인라인화
□ 스켈레톤 UI 적용
□ 이미지 lazy loading
□ 폰트 최적화 (font-display: swap)

[런타임 최적화]
□ React.memo 적절한 사용
□ useMemo/useCallback 최적화
□ Virtual List (긴 목록)
□ Web Worker (무거운 연산)

```

### Nginx 최적화 설정

```
# HTTP/2 활성화
server {
    listen 443 ssl http2;

    # Brotli 압축 (Gzip보다 효율적)
    brotli on;
    brotli_types text/plain text/css application/json application/javascript;

    # 연결 유지
    keepalive_timeout 65;

    # 버퍼 최적화
    client_body_buffer_size 10K;
    client_header_buffer_size 1k;
    client_max_body_size 8m;
    large_client_header_buffers 4 32k;

    # 정적 파일 sendfile
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
}

```

---

## 📊 요약 비교표

| 항목 | CSR | SSR |
|: -----|:----|:----|
| **First Paint** | 느림 (JS 로드 후) | 빠름 (HTML 즉시) |
| **TTI** | First Paint와 동시 | Hydration 후 |
| **서버 비용** | 낮음 | 높음 |
| **SEO** | 불리 | 유리 |
| **개발 복잡도** | 낮음 | 높음 |
| **인프라** | 정적 호스팅 가능 | Node.js 서버 필요 |
| **캐싱** | CDN 캐싱 용이 | 동적 콘텐츠 캐싱 어려움 |
| **초기 로딩** | 무거움 | 가벼움 |
| **이후 네비게이션** | 빠름 | 빠름 |

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CSR 선택 기준:
✅ SEO 불필요 (로그인 필수 서비스)
✅ 복잡한 클라이언트 인터랙션
✅ 정적 호스팅으로 비용 절감
✅ PWA/오프라인 기능 필요

SSR/SSG 선택 기준:
✅ SEO 중요 (콘텐츠 사이트)
✅ 빠른 First Paint 필수
✅ 소셜 미디어 공유 메타태그
✅ 저사양 기기 지원

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```

┌─────────────────────────────────────────────────────────────────────────┐
│                    CSR이 유리한 이유 요약                                │
└─────────────────────────────────────────────────────────────────────────┘

1. SEO 불필요 (로그인 필수 서비스)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    
    SSR의 핵심 장점인 "SEO"가 무의미해짐
    → 크롤러는 어차피 로그인 못 함
    → SSR의 복잡도/비용만 남고 이점은 사라짐
    → CSR로 충분!
    
2. 복잡한 클라이언트 인터랙션
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    
    SSR:  보이지만 동작 안 하는 "어색한 구간" 발생
    CSR: 보이는 순간 = 동작하는 순간
    
    - 클라이언트 전용 API (Canvas, WebGL, WebSocket)
    - 즉각적인 피드백 (Optimistic Update)
    - 60fps 애니메이션
3. 정적 호스팅으로 비용 절감
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    
    SSR: 서버 24/7 운영 필요 → $100+/월
    CSR: CDN 정적 호스팅 → $0~20/월
    
    - 서버 관리 불필요
    - 자동 스케일링
    - 장애 위험 최소화

결론:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

"SSR의 장점을 활용할 수 없는 상황에서는
SSR의 단점(복잡도, 비용)만 떠안게 됨"

→ 로그인 필수 + 복잡한 인터랙션 = CSR이 최적의 선택

┌─────────────────────────────────────────────────────────────────────────┐
│                    React 초기화 방식 비교                                │
└─────────────────────────────────────────────────────────────────────────┘

CSR:   createRoot() + render()
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

초기 HTML:
┌─────────────────────────────┐
│  <div id="root"></div>      │  ← 비어있음!
└─────────────────────────────┘

React 코드:
┌─────────────────────────────────────────────────┐
│  import { createRoot } from 'react-dom/client'  │
│                                                 │
│  const root = createRoot(                       │
│    document.getElementById('root')              │
│  )                                              │
│  root.render(<App />)                           │
│                                                 │
│  // React가 DOM을 "새로 생성"함                  │
└─────────────────────────────────────────────────┘

결과:
┌─────────────────────────────┐
│  <div id="root">            │
│    <div class="app">        │  ← React가 생성한 DOM
│      <button>Click</button> │
│    </div>                   │
│  </div>                     │
└─────────────────────────────┘

SSR:  hydrateRoot()
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

초기 HTML (서버가 생성):
┌─────────────────────────────┐
│  <div id="root">            │
│    <div class="app">        │  ← 이미 내용이 있음!
│      <button>Click</button> │
│    </div>                   │
│  </div>                     │
└─────────────────────────────┘

React 코드:
┌─────────────────────────────────────────────────┐
│  import { hydrateRoot } from 'react-dom/client' │
│                                                 │
│  hydrateRoot(                                   │
│    document.getElementById('root'),             │
│    <App />                                      │
│  )                                              │
│                                                 │
│  // React가 기존 DOM을 "재사용"하며 연결         │
└─────────────────────────────────────────────────┘

결과:
┌─────────────────────────────┐
│  <div id="root">            │
│    <div class="app">        │  ← 같은 DOM, 이벤트만 연결됨
│      <button>Click</button> │     (DOM 재생성 없음)
│    </div>                   │
│  </div>                     │
└─────────────────────────────┘

```jsx
┌─────────────────────────────────────────────────────────────────────────┐
│                         핵심 정리                                        │
└─────────────────────────────────────────────────────────────────────────┘

                    CSR                         SSR
                     │                           │
    초기 HTML     빈 <div>                   완성된 콘텐츠
                     │                           │
    React 함수    createRoot()              hydrateRoot()
                  + render()                     │
                     │                           │
    동작          DOM 새로 생성              기존 DOM에 연결
                     │                           │
    Hydration       없음 ❌                    있음 ✅
                     │                           │
    Mismatch        불가능                     발생 가능
    문제

Hydration이 SSR에서만 필요한 이유:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CSR:   브라우저에서 React가 DOM을 직접 생성
      → "연결"할 기존 DOM이 없음
      → Hydration 개념 자체가 불필요

SSR:   서버가 HTML을 미리 생성해서 보냄
      → 브라우저에 이미 DOM이 존재
      → React가 이 DOM을 "인수인계" 받아야 함
      → 이 과정이 Hydration
```

```jsx
┌─────────────────────────────────────────────────────────────────────────┐
│                         핵심 정리                                        │
└─────────────────────────────────────────────────────────────────────────┘

                    React (순수)              Next.js
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

정체               UI 라이브러리              풀스택 프레임워크

렌더링             CSR만                     SSG/SSR/ISR/CSR 선택

초기 HTML          항상 비어있음              콘텐츠 포함 (프리렌더링)

Hydration          없음                      있음 (기본)

라우팅             별도 라이브러리            파일 기반 (내장)

API                별도 백엔드               Route Handlers 내장

배포               정적 호스팅               Node.js 서버 (또는 정적)

SEO                불리                      유리

결론: 
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Next.js = React + 서버 렌더링 + 라우팅 + API + 최적화

- React는 "뷰를 그리는 방법"만 제공
- Next.js는 "앱을 만드는 전체 구조"를 제공
- Hydration은 서버에서 미리 렌더링하기 때문에 필요한 개념
- CSR만 쓰면 Hydration이 필요 없음 (React와 동일)
```

네, 질문하신 **"렌더링 타이밍"**과 **"하이드레이션(Hydration) 여부"**는 CSR과 SSR의 차이를 설명하는 아주 핵심적인 키워드입니다. 틀린 말이 아니라 오히려 **기술적으로 매우 정확한 지점**을 짚으신 거예요.

조금 더 명확하게 두 개념을 비교해 드릴게요.

---

### 1. 렌더링 타이밍 (Rendering Timing)

누가, 언제 HTML을 완성하느냐의 차이입니다.

| 구분 | CSR (Client Side Rendering) | SSR (Server Side Rendering) |
| --- | --- | --- |
| **주체** | **브라우저(클라이언트)** | **서버** |
| **타이밍** | 브라우저가 JS 파일을 다 다운로드한 후 | 사용자가 요청한 즉시 (실시간 생성) |
| **초기 응답** | 빈 HTML (`<div id="root"></div>`) | 데이터가 채워진 완성된 HTML |
- **CSR**은 일단 빈 도화지를 먼저 받고, 브라우저가 자바스크립트를 실행해서 그 위에 그림을 그립니다.
- **SSR**은 서버에서 이미 그림을 다 그린 뒤 완성된 도화지를 브라우저에 던져줍니다.

---

### 2. 하이드레이션 (Hydration) 여부

하이드레이션은 **"정적인 HTML에 자바스크립트를 입혀서 상호작용이 가능하게 만드는 과정"**입니다.

- **SSR의 필수 단계:** 서버에서 받은 HTML은 겉모습은 완성되어 있지만, 버튼을 눌러도 반응하지 않는 '껍데기' 상태입니다. 브라우저가 JS를 읽고 기존 HTML에 이벤트 리스너를 결합하는 **하이드레이션 과정이 반드시 필요**합니다.
- **CSR과의 차이:** CSR은 애초에 JS가 DOM을 처음부터 생성하므로 '기존 HTML에 생기를 불어넣는' 하이드레이션이라는 개념보다는 **'전체 렌더링'** 과정 속에 포함되어 있다고 봅니다. 반면, SSR은 **TTV(볼 수 있는 시점)**와 **TTI(클릭 가능한 시점)** 사이에 하이드레이션이라는 공백이 존재한다는 것이 큰 특징입니다.

---

### 요약하자면

- **렌더링 타이밍:** "서버에서 미리 그려서 오느냐(SSR), 브라우저에서 직접 그리느냐(CSR)"의 차이
- **하이드레이션:** "완성된 HTML에 나중에 기능을 붙이느냐(SSR), 처음부터 기능을 포함해서 그리느냐(CSR)"의 차이

> TIP:
 면접이나 기술 논의 시에는 **"TTV(Time to View)와 TTI(Time to Interact)의 간극"**이라는 
용어를 곁들이면 훨씬 전문적으로 들립니다. SSR은 TTV는 빠르지만 하이드레이션 때문에 TTI까지 시간이 걸린다는 점이 핵심이거든요.
> 

혹시 이 과정에서 발생하는 **SEO(검색엔진 최적화)** 문제나 **서버 부하** 차이에 대해서도 더 궁금하신가요?

> 저희 프로젝트가 운영툴이라는 점에 주목했습니다. 운영툴은 SEO가 불필요하고, 한 번 접속하면 장시간 머무르며 빈번한 데이터 조작이 일어납니다. 따라서 서버 부하를 높이는 SSR보다는, 초기 로딩 이후 앱과 같은 부드러운 경험을 주는 CSR이 비용과 운영 측면에서 효율적이라고 판단했습니다. SSR의 TTI 간극 문제(하이드레이션 지연)를 해결하기 위해 복잡한 최적화 기술을 도입하는 대신, 그 리소스를 운영툴의 핵심 기능 고도화에 사용하기로 결정했습니다."
> 

---

**추가 팁:** 만약 면접관이 "그래도 초기 로딩 속도가 너무 느리면 어떡하냐?"고 물어본다면, **"Code Splitting(코드 분할)"**과 **"브라우저 캐싱"**을 통해 CSR의 단점을 보완했다고 답변하시면 완벽합니다.

- **`gzip on;` (초기 로딩 속도 보완)**
    - CSR은 자바스크립트(JS) 파일이 커지면 초기 로딩이 느려집니다. Gzip 압축을 통해 JS 파일을 압축해서 전송함으로써 **네트워크 전송 시간을 대폭 단축**시킵니다.
- **`location ~* \.(js|css|...)$`의 캐싱 설정 (재방문 시 성능 보완)**
    - `expires 1y;`와 `Cache-Control "public, immutable";` 설정은 브라우저가 정적 파일을 1년 동안 로컬에 저장하게 만듭니다. 덕분에 **재방문 시에는 서버에 요청조차 보내지 않고** 즉시 앱이 실행됩니다.
- **`try_files $uri $uri/ /index.html;` (SPA 라우팅 문제 해결)**
    - CSR은 페이지가 하나(`index.html`)뿐이라 브라우저에서 `/settings` 같은 주소로 직접 접속하면 404 에러가 납니다. 이 설정은 어떤 경로로 들어오든 `index.html`을 먼저 보여주어 **JS가 라우팅을 가로챌 수 있게** 해주는 필수 설정입니다.
- **`proxy_pass` (CORS 문제 및 보안 보완)**
    - 브라우저가 직접 API 서버로 요청을 보내면 CORS 설정이 복잡해질 수 있는데, Nginx가 중간에서 대리인(Proxy) 역할을 수행하여 **동일 출처(Same-origin) 정책**을 유지하면서 데이터를 안전하게 가져오게 합니다.

## **에러 원인: react-refresh/only-export-components**

- *React Fast Refresh (HMR)**는 파일이 **컴포넌트만** export할 때 정상 동작합니다.

```jsx
🎯 대용량 시나리오: 가상화 리스트 10000행
  ├─ 기존 (includes): 1.20ms
  ├─ 개선 (Set.has): 0.416ms
  └─ Set이 3배 빠름 🚀

stdout | src/utils/benchmark.test.ts > Array.includes vs Set.has 성능 벤치마크 > Set 생성 비용 포함 분석 > Set 생성 비용을 포함해도 이득인 최소 검색 횟수

  💰 Set 생성 비용 분석 (배열 1000개)
  ├─ Set 생성 비용: 0.093ms
  ├─ 평균 includes 비용: 0.0005ms
  ├─ 평균 has 비용: 0.0001ms
  └─ 손익분기점: 227회 이상 검색 시 Set이 유리

  💡 useMemo 사용 시 Set 생성은 의존성 변경 시에만 발생
     → 대부분의 경우 Set.has가 유리
```

[다국어 관련 렌더링 최적화 ](Render/%EB%8B%A4%EA%B5%AD%EC%96%B4%20%EA%B4%80%EB%A0%A8%20%EB%A0%8C%EB%8D%94%EB%A7%81%20%EC%B5%9C%EC%A0%81%ED%99%94%202edcee38e89a80e7bc00d2aaacc3d497.md)
